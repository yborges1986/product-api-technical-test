# Product Service - Configuración de Ejemplo
# Copia este archivo como .env y ajusta los valores según tu entorno

# ===========================================
# CONFIGURACIÓN DEL SERVIDOR
# ===========================================
PORT=3000
NODE_ENV=development

# ===========================================
# BASE DE DATOS MONGODB
# ===========================================
# Para Docker (recomendado):
MONGODB_URI=mongodb://mongo:27017/product_management

# Para desarrollo local (sin Docker):
# MONGODB_URI=mongodb://localhost:27017/product_management

# Para testing:
# MONGODB_URI=mongodb://localhost:27017/treew_test

# ===========================================
# CONFIGURACIÓN JWT
# ===========================================
JWT_SECRET=treew-super-secret-jwt-key-2024
JWT_EXPIRES_IN=24h
JWT_AUDIENCE=treew-app
JWT_ISSUER=treew-product-api

# ===========================================
# ENCRIPTACIÓN
# ===========================================
BCRYPT_ROUNDS=12
# Para testing usar BCRYPT_ROUNDS=4 (más rápido)

# ===========================================
# MESSAGING NATS
# ===========================================
# Para Docker (recomendado):
NATS_URL=nats://nats:4222

# Para desarrollo local (sin Docker):
# NATS_URL=nats://localhost:4222

# ===========================================
# AUTENTICACIÓN ENTRE SERVICIOS
# ===========================================
SERVICE_TOKEN=treew-internal-secret-2024

# ===========================================
# CONFIGURACIONES ADICIONALES
# ===========================================
# Logging level (debug, info, warn, error)
LOG_LEVEL=info

# Rate limiting (requests per minute)
RATE_LIMIT=100

# CORS Origins (separados por coma)
CORS_ORIGINS=http://localhost:3000,http://localhost:4002

# Health check interval (en segundos)
HEALTH_CHECK_INTERVAL=30
